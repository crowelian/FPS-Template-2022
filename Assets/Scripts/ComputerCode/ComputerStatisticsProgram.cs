using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

// Too specific code... fix this!
public class ComputerStatisticsProgram : MonoBehaviour
{

    // [SerializeField]
    // private Text statusText;
    // private GameManager gm = null;

    // // Start is called before the first frame update
    // void Start()
    // {
    //     gm = GameObject.FindGameObjectWithTag("GameController").GetComponent<GameManager>();
    // }

    // // Update is called once per frame
    // void Update()
    // {
    //     statusText.text = "Power: \n" + gm.GetUsage() + "% used of " + gm.GetVoltage() + "%";

    //     if (GetComponent<ProgramOnDisk>())
    //     {
    //         if (GetComponent<ProgramOnDisk>().computerRunningThisSoftware.computerInFocus)
    //         {
    //             if (Input.GetKeyUp(KeyCode.Q))
    //             {
    //                 GetComponent<ProgramOnDisk>().ProgramExit();
    //             }
    //         }
    //     }



    // }
}